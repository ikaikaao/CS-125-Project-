#include <stdio.h>
#include <stdlib.h>
#include <time.h> 

#define size 6

int main() {
    
   int comp_board[size][size], i, i2;
   
   for (i=0; i<6; i++) {
     for (i2=0; i2<6, i2++) {
         
        if (numrand == 0) {
          comp_board[i][i2] = 0;
          continue; 
        }
          
        if (numrand == 1) {
          switch (boat_size) 
          case 1: 
            fourBoats(*comp_board); 
            break;
          case 2:
            threeBoats(*comp_board);
            break;
          case 3:
            twoBoats(*comp_board);
            break;
          
        }
          
     } 
   }
   
}
